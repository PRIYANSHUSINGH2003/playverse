import SectionTitle from '../components/SectionTitle';
export default function About() { return <main className="container page-content prose-page"><SectionTitle eyebrow="THE IDEA" title="About PlayVerse"/><div className="about-grid"><div><p className="lead">PlayVerse is designed around one simple problem: game discovery is often more work than playing the game.</p><p>The platform combines a curated browser-game layer with optional live catalog providers, giving players a fast way to search, filter, save, and launch titles from one responsive interface.</p><p>The interface intentionally separates PlayVerse UI from third-party game content. That matters because external sites can choose to block iframe embedding with security headers, so the player falls back to a direct launch instead of pretending every game can run inside the page.</p></div><div className="principles"><div><b>01</b><span>Fast discovery</span></div><div><b>02</b><span>Responsive by default</span></div><div><b>03</b><span>Safer external launches</span></div><div><b>04</b><span>Local-first preferences</span></div></div></div></main>; }
