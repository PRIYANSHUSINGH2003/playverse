import { useState } from 'react';
import { submitContact } from '../lib/api';
import Icon from '../components/Icon';
import SectionTitle from '../components/SectionTitle';
export default function Contact() { const [form,setForm]=useState({name:'',email:'',message:''}); const [state,setState]=useState({busy:false,error:'',success:''}); const onSubmit=async(e)=>{e.preventDefault();setState({busy:true,error:'',success:''});try{const data=await submitContact(form);setState({busy:false,error:'',success:data.message});setForm({name:'',email:'',message:''});}catch(err){setState({busy:false,error:err.message,success:''});}}; return <main className="container page-content contact-page"><SectionTitle eyebrow="GET IN TOUCH" title="Contact PlayVerse"/><div className="contact-grid"><div className="contact-copy"><p className="lead">Found a broken launch, a missing game, or a UX issue?</p><p>Send the details and use the message field to include a title or URL. The bundled API validates the request; connect it to your email or ticket provider before production deployment.</p><div className="contact-card"><strong>Tip</strong><span>Never send passwords, payment details, or other sensitive information through a game-site support form.</span></div></div><form className="contact-form" onSubmit={onSubmit}><label>Name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required minLength={2} maxLength={80} placeholder="Your name"/></label><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required placeholder="you@example.com"/></label><label>Message<textarea value={form.message} onChange={e=>setForm({...form,message:e.target.value})} required minLength={10} maxLength={3000} rows={7} placeholder="Tell us what you found..."/></label><button className="primary-btn" disabled={state.busy}>{state.busy?'Sending...':<>Send message <Icon name="arrow" size={16}/></>}</button>{state.error&&<p className="form-error" role="alert">{state.error}</p>}{state.success&&<p className="form-success" role="status">{state.success}</p>}</form></div></main>; }
